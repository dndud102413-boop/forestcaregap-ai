# ForestCareGap AI — Streamlit Community Cloud 배포용 의존성
# 핵심: pandas/numpy/sklearn 을 '2.x 안정 조합'으로 상한 고정
#      (상한이 없으면 클라우드가 pandas 3.0 등 최신을 깔아서 앱이 크래시함)

streamlit>=1.30,<2.0
pandas>=2.2,<3.0          # ★ pandas 3.0 은 2.x 코드와 호환 안 됨 → 반드시 <3.0
numpy>=1.26,<2.0          # 모델(pickle) 호환 위해 numpy 1.x 고정
pyarrow>=14.0             # forest_scored.parquet 읽기 (필수)
scikit-learn>=1.3,<2.0    # ml_engine.joblib 로드. 로드 에러 시 로컬과 동일 버전으로 == 고정
joblib>=1.3

# 지도 (산림공백 시군구 단계구분도 — 데모 핵심 비주얼)
geopandas>=0.14,<2.0
shapely>=2.0
pydeck>=0.9

# 차트 (없으면 streamlit 내장 차트로 자동 대체)
plotly>=5.18

# PDF 보고서 (없으면 해당 버튼만 비활성)
reportlab>=4.0

# AI 산주 리포트 (Gemini). 키 없어도 앱은 폴백 텍스트로 정상 작동
google-genai>=0.3
