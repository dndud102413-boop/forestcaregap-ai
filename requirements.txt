# ForestCareGap AI — Streamlit Community Cloud 배포용 의존성
# (로컬 requirements 에서 누락돼 있던 pyarrow / pydeck / reportlab / google-genai / joblib 추가)

streamlit>=1.30
pandas>=2.0
numpy>=1.24,<2.0        # 모델(pickle) 호환 위해 numpy 1.x 고정
pyarrow>=14.0           # forest_scored.parquet 읽기 (필수 — 없으면 앱 시작 실패)
scikit-learn>=1.3       # ml_engine.joblib 로드. 모델 로드 에러 시 로컬과 동일 버전으로 == 고정
joblib>=1.3             # 모델 로드

# 지도 (산림공백 시군구 단계구분도 — 데모 핵심 비주얼)
geopandas>=0.14
shapely>=2.0
pydeck>=0.9

# 차트 (없으면 streamlit 내장 차트로 자동 대체)
plotly>=5.18

# PDF 보고서 (없으면 해당 버튼만 비활성)
reportlab>=4.0

# AI 산주 리포트 (Gemini). 키 없어도 앱은 폴백 텍스트로 정상 작동
google-genai>=0.3
